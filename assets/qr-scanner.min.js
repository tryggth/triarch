/**
 * Lightweight QR Scanner integration bundle
 * TRIARCH: Cyclic Edge PWA
 */

export class QRScannerHelper {
  static isCameraSupported() {
    return !!(navigator.mediaDevices && navigator.mediaDevices.getUserMedia);
  }
}
